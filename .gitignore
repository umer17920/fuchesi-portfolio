# dependencies
node_modules/

# Next.js build output
.next/
out/
tsconfig.tsbuildinfo

# scratch + generated artifacts — must never be pushed. tmp/ holds screenshots,
# a local python doc runtime, and CV/document renders (some private); output/
# holds generated documents.
tmp/
output/

# local environment and secrets
.env
.env.local
.env.*.local

# logs and editor files
*.log
.DS_Store
Thumbs.db
.vscode/

# local Codex workspace metadata
.agents/
.codex/

# agent skill symlinks: absolute paths into .agents/, machine-specific.
# Regenerate from the tracked skills-lock.json with `npx skills install`.
.claude/skills/

# unrelated local webhook prototype (kept in workspace, not published)
src/
scripts/
.env.example
report.html
